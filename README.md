# youcode-lists-objects

A tool to check your arrays and objects exercises 

Usage:

- `yarn install`

- fill in all of the functions with a pattern of '`<functionName>`.js'

- run `node testFunctions`

- enjoy your fast feedback ;) 
